<!DOCTYPE html>
<html >
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      
    <title>Restuarant.terebinth</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
 </head>

    <body>
     
<!-- Navigation -->

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Restaurant-Terebinth</a>
    </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
        <li><%=link_to "Meatdish",menu_lists_path%></li>
         <li> <%=link_to "Drinking", drinking_menu_lists_path%></li>
        <li><%=link_to "Dessert",dessert_menu_lists_path%></li>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
       
        <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= current_user.email%> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
          <li><%= link_to "Edit",edit_users_path%>  </li>
           
            <!--Check Superuser ? -->
        <%if current_user.super_user?%> 
          <li><%= link_to "Show all User",showusers_showusers_path%></li>
          
        <%end%>

         <li><%= link_to destroy_user_session_path do %><i class="glyphicon glyphicon-log-in"></i> &nbsp; Logout
         <%end%>

        <%end%>
          </ul>
        </li>
       
        </li>
      </ul>
    </div>
</nav>

     <div class="container main-body">
    <div id="system-flashes">
      <%- [:notice, :info, :warning, :error, :alert].each do |kind| -%>
        <%- next if flash[kind].blank? -%>
        <%
          alert_type = case kind
            when :notice
              'success'
            when :error
              'danger'
            when :info
              'info'
            when :warning
              'warning'
            when :alert
              'warning'
            end
        %>
        <div class="alert alert-dismissable alert-<%= alert_type %> notice fade in">
          <button class="close" data-dismiss="alert" type="button"><span class="glyphicon glyphicon-remove"></span></button>
          <% [flash[kind]].flatten.each do |line| %>
            <div><%= line.html_safe %></div>
          <% end %>
        </div>
      <%- end -%>
    </div>


        <%= yield %>
      </div>

</body>
</html>