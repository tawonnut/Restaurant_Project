



<% if @menu != nil%>
<%= @menu.first.table.restuarant.restuarant_name %>
<%= current_user.name %>
<%= @menu.first.table%>
<%= Time.now%>
 <div class="panel panel-default">
      <div class="panel-heading"><h4>Print เครื่องดื่ม<h4></div>
        <div class="panel-body">
          <table class="table table-striped" id="show_datal">
            <thead>
            <tr>
              <th>#</th>
              <th>ชื่ออาหาร</th>
              <th>รายละเอียด</th>
              <th>จำนวน</th>
            </tr>
          </thead>
          <tbody>
              <% @menu.each_with_index do |a, i| %>
              <tr>
                <td><%= i + 1 %></td>
                <td><%=a.menu%></td>
                <td><%=a.remark%></td>
                <td><%=a.value%></td>
              </tr> 
              <%end%>
              </div>  
              </tbody>
          </table>
        </div>
      </div>
  </div>
<%end%>